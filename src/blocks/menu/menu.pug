mixin menu(...items)
	nav.menu&attributes(attributes)
		ul.menu__list
			- for (var i = 0; i < items.length; i++)
				if i == 0
					li.menu__item.menu__item--active
						a.text--sm.menu__link(href="")= items[i]
				else
					li.menu__item
						a.text--sm.menu__link(href="")= items[i]
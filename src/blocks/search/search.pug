mixin search(data)
	div.search(class!=attributes.class)
		+input({label_text: data.label_text, type: data.type ,name: data.name  , placeholder: data.placeholder})
		
		+btn({icon_l: data.icon, text: data.text, type: 'submit'})